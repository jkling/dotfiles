# Dotfiles / New Machine Bootstrap

Written for Debian/Ubuntu; may have some redundant installs in an Ubuntu environment.

Root folder contains scripts and config files for general PC setup; use subfolder installers for different development environments.

1. /install.sh for a basic install: (just symlinks bashrc and gitconfig)
